# API endpoint - Leave empty for local development (uses Vite proxy)
# VITE_API_URL=

# For production, set to your backend URL (no trailing slash, no /api prefix)
# Example: VITE_API_URL=https://ai-lighting-api.onrender.com
